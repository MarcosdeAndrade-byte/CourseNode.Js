<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head') %>
<body>

    <%- include('./partials/nav.ejs') %>

    <!-- Podemos utilizar loops nos arquivos EJS/Dessa forma podemos imprimir os dados vindos do servidor -->
    <% if (blogs.length > 0) { %>
        <% blogs.forEach(blogs => { %>
            <h3 class="title"><%= blogs.title %></h3>
            <p class="snippet"><%= blogs.snippet %></p>
        <%})%>
    <%}%>

    <!--Podemos substituir partes do html e colocar em um único arquivo-->
    <!--Para incluir as partes no arquivo principal utilizamos a função include-->
    <%- include('./partials/footer.ejs') %>
</body>
</html>